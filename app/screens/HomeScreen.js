import React from 'react';
import {
  Text,
  Button,
  StyleSheet,
  View,
  TextInput,
  FlatList,
} from 'react-native';

export default () => {
  return <Text>Hello from Home screen</Text>;
};

const styles = StyleSheet.create({});
